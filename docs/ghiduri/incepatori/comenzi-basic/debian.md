# Actualizare listă pachete
```bash
sudo apt update && sudo apt upgrade
```
# Căutare pachete
```bash
apt search nume-pachet
```
# Instalare aplicație (ex: Firefox)
```bash
sudo apt install firefox
```
# Ștergere aplicație
```bash
sudo apt remove firefox
```
